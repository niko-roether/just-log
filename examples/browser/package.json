{
  "$schema": "https://json.schemastore.org/package",
  "name": "example-browser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "yarn@4.5.0",
  "scripts": {
    "start": "esbuild ./src/index.ts --bundle --outfile=dist/index.js && cp index.html dist/ && tsx-serve ./dist"
  },
  "dependencies": {
    "just-log": "workspace:^",
    "just-log-browser": "workspace:^"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tsx-serve": "^1.1.19",
    "typescript": "^5.6.3"
  }
}
