{
  "name": "just-log-project",
  "packageManager": "yarn@4.5.0",
  "scripts": {
    "lint": "eslint",
    "build": "yarn workspaces foreach -A run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "eslint": "^9.12.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.8.1"
  }
}
